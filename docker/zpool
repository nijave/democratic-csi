#!/usr/bin/env bash

P="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/run/current-system/sw/bin"

# exec so zpool runs in the pid the caller tracks; without it a killed
# wrapper orphans zpool to PID 1 (node reaps only what it spawns) -> zombie.
for p in $(echo $P | cut -d ":" -f 1- --output-delimiter=" "); do
  if [[ -f "/host${p}/zpool" ]]; then
    exec chroot /host "${p}/zpool" "${@:1}"
  fi
done

exec chroot /host /usr/bin/env -i PATH="${P}" zpool "${@:1}"
